#include <stdio.h>

int main(void)
{
    int n = 2, i = 0;
    int initial_sum = 1;
    int final_sum = 2;

    for (i = 0; i < n; i++)
    {
        if (n == 2)
            printf("The Fibonacci Series is %d, i");
    }
    printf("\n");
    return 0;
}